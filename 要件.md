# 要件

## 概要

node/npmの「JSON Server」を使用して、ローカルテスト用のWebAPIサーバーのモックサーバーを作成したい。

## やりたいこと

やりたいことの一覧です。

- 更新系のAPIを実行するとdb.jsonが更新されるが、サーバー起動時の初期状態のdb.jsonは残したい。
- GET APIは、データ取得処理をデフォルトからカスタマイズできるようにしたい。
  - モック対象のAPIのパラメータと動作を合わせられるようにしたい。
- リクエストを受け取ったときのログを残したい。
  - 特に残したいのは、受信日時と一つ前のリクエストとの間隔です。
  - モック対象のサーバーの制限事項に、「1秒間当たり10リクエストまで」というものがあり、この制限を超えていないかをテストするためです。
